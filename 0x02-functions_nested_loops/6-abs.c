#include <stdio.h>
#include "main.h"

/**
 * _abs - Entry point
 * @x: parameter which absolute value will be given
 * this function is to compute the absolute value of an integer.
 * Return: 0 Always - Success
 */

int _abs(int x)
{
	int n;

	n = abs(x);
	_putchar('n');
	return (0);
}
