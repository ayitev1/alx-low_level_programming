#include <stdio.h>
#include "main.h"

/**
 * _abs - Entry point
 *
 * this function is to compute the absolute value of an integer.
 * Return: 0 Always - Success
 */

int _abs(int)
{
	int n;

	n = abs(int);
	_putchar('n');
	return (0);
}
