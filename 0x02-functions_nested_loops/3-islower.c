#include "main.h"

/**
 * islower - Entry point
 *
 * this function is to check for lowercase character
 * Return: 0 Always - Success
 */
int _islower(int c);
{
	int c;

	if (_islower(c))
	{
		_putchar(1);
		else
		{
			_putchar(0)
		}
	}
	return (0);
}
