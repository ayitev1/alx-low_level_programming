#include <stdio.h>
#include "main.h"

/**
 * _abs - Entry point
 *
 * this function is to compute the absolute value of an integer.
 * Return: 0 Always - Success
 */

int _abs(int)
{
	int i;

	if (n > 0)
	{
		i = 1;
		_putchar('+');
	}
	else if (n < 0)
	{
		i = -1;
		_putchar('-');
	}
	else
	{
		i = 0;
		_putchar('0');
	}
	return (i);
}
