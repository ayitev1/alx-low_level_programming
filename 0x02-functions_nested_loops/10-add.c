#include "main.h"

/**
 * add - Entry point
 * @x: first operand ofparameters to add
 * @y: second operand ofparameters to add
 * this function is to add two integers and returns the result
 *
 * Return: 0 Always - Success
 */

int add(int x, int y)
{
	int n = 0;

	n = x + y;
	return (n);
}
